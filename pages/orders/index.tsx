import type { NextPage } from "next"
import Head from 'next/head'

const Orders: NextPage = () => {
    return (<>
        <Head>
            <title>Заявки</title>
        </Head>
    </>);
}

export default Orders;
